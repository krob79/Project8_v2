extends layout

block content
    h1=`Book Details`
    form(method='post' action=`./${book.id}`)
        p
            label(for=`title`)=`Title`
            input(name="title" type="text" id="title" value=book.title)
        p
            label(for=`author`)=`Author`
            input(name="author" type="text" id="author" value=book.author)
        p
            label(for=`genre`)=`Genre`
            input(name="genre" type="text" id="genre" value=book.genre)
        p
            label(for=`year`)=`Year`
            input(name="year" type="number" id="year" value=book.year)
        p
            input(type="submit" value="Update Book")
    form(method='post' action=`./${book.id}/delete` onsubmit="confirm(`Do you really want to delete this?`)")
        p
            input(type="submit" value="Delete Book")
            a(class="button" href="/books/")=`Cancel`
    include ./scripttag.pug